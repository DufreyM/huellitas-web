DATABASE_URL="postgresql://user:password@localhost:5432/huellitas"
PORT_BK=3001
JWT_SECRET="replace-with-a-long-random-secret"
JWT_EXPIRES_IN="1d"

# URL pública del frontend, usada para construir el enlace de recuperación de contraseña.
FRONTEND_URL="http://localhost:5173"

# Envío de correo (recuperación de contraseña). Si se dejan vacías, el enlace se
# imprime en la consola del servidor en lugar de enviarse por correo.
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
EMAIL_FROM="Huellitas de la Calle <no-reply@huellitas.org>"
